name: Build EXE and Release

on:
  workflow_dispatch:   # manual trigger ONLY (safe)
  push:
    branches:
      - main
  
permissions:
  contents: write 
  
jobs:
  build:
    runs-on: windows-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12.10'  # Adjust as needed

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 5. Build EXE with PyInstaller
      - name: Build EXE
        run: |
          pip install pyinstaller  # ensure PyInstaller is installed
          pyinstaller --onefile --noconsole --icon=logo/logo.ico app.py

      # 6. Create GitHub Release and upload EXE
      - name: Upload EXE to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Release ${{ github.run_number }}
          files: dist/app.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

